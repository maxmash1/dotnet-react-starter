# Swagger Configuration

This directory contains Swagger/OpenAPI customization files.

Swagger is configured in `Program.cs` with XML documentation support.

## Usage

Access the Swagger UI at `/swagger` when running in Development mode.
